You are a senior product engineer, UI/UX strategist, and growth-focused web architect.

I have an outdated college website: https://ymkcoe.com/index.php

The current system has:
- PHP-based pages (no SPA, no API layer)
- Static content across multiple pages (About, Courses, Faculty, Admissions)
- Heavy use of PDFs instead of structured content
- Repeated HTML structure across pages (no component reuse)
- Bootstrap + jQuery stack (outdated)
- Duplicate URLs (http, https, index.php)
- Poor mobile experience and weak UI hierarchy

Your task is to completely redesign and rebuild this into a modern, scalable MERN-based platform.

---

## PHASE 1: CRITICAL AUDIT

Break down the current system and identify:

1. Architecture flaws
   - Lack of API layer
   - Tight coupling between UI and content
   - No modularity

2. UX/UI problems
   - Poor navigation flow
   - Weak CTA placement (no conversion focus)
   - Content buried inside PDFs
   - No visual hierarchy

3. Performance issues
   - Redundant CSS/JS
   - No lazy loading
   - No caching strategy

4. SEO issues
   - Duplicate URLs
   - Poor URL structure (.php routes)
   - Missing structured internal linking

---

## PHASE 2: SYSTEM REDESIGN (MERN)

Design a full system with:

### FRONTEND (React + Vite or Next.js)
- Fully component-based architecture
- Pages:
  - Home (conversion-focused)
  - About
  - Courses (dynamic)
  - Faculty (dynamic)
  - Admissions
  - Placements
  - News & Media
  - Contact

- Features:
  - Responsive design (mobile-first)
  - Clean UI (modern spacing, typography)
  - Smooth animations (but minimal)

---

### BACKEND (Node.js + Express)

Design REST APIs:

- /api/courses
- /api/faculty
- /api/admissions
- /api/news
- /api/media
- /api/contact

Include:
- JWT-based admin authentication
- File upload system (replace PDFs with structured data + optional downloads)

---

### DATABASE (MongoDB)

Create schemas:

- Courses
- Faculty
- Announcements
- Documents
- Admissions Leads
- Admin Users

Ensure:
- Normalized structure
- Easy extensibility

---

## PHASE 3: ADMIN DASHBOARD (THIS IS THE REAL POWER)

Build a CMS-style admin panel:

- Add/Edit/Delete:
  - Courses
  - Faculty
  - Announcements
  - Media
- Upload PDFs/images
- Control homepage sections dynamically
- Track inquiries/leads

---

## PHASE 4: UX UPGRADE (THIS IS WHERE VALUE COMES)

Replace:

❌ PDFs  
→ ✅ searchable structured pages

Add:
- Smart search (courses, faculty)
- Filters (department, specialization)
- Clear CTAs:
  - Apply Now
  - Download Brochure
  - Contact Admissions

Navigation:
- Sticky navbar
- Mega menu for departments

---

## PHASE 5: PERFORMANCE & SEO

- Enforce HTTPS
- Remove duplicate URLs
- Clean routing (no .php)
- Add:
  - Meta tags
  - Open Graph
  - Structured schema
  - Clean sitemap

Optimize:
- Lazy loading
- Image compression
- CDN usage

---

## PHASE 6: CONVERSION ENGINE (DON’T SKIP THIS)

Turn this into a lead machine:

- Add admission inquiry form (store in DB)
- Add WhatsApp integration
- Add chatbot for FAQs
- Add placement stats dashboard
- Add testimonials & social proof

---

## OUTPUT FORMAT

Give me:

1. Complete system architecture (text diagram)
2. Folder structure (frontend + backend)
3. MongoDB schema design
4. API routes
5. Key React components
6. Step-by-step migration plan (PHP → MERN)
7. UI/UX redesign suggestions (practical, not generic)
8. Conversion optimization ideas (admissions-focused)

Be practical, production-ready, and specific.
Avoid generic explanations.